spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myblog
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      #更新或创建数据表
      #ddl-auto: update
    #控制台显示SQL
    show-sql: true